const btn = document.querySelector('#btn');

if (btn) {
    btn.addEventListener('click', () => {
        fetch ('/room6')
        .then ((response) => response.json())
        .then((data) =>{
            const 
        })
    })
}
