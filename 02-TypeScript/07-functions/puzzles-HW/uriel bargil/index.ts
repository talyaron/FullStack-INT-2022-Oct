// // level 1
// // 2.
// function square(x: number): number {
//     return x * x;
//   }
//   const result = square(3);
//   console.log (result);

// // 3.
// function celsiusToFahrenheit(celsius: number): number {
//     return celsius * (9/5) + 32;
//   }
//   const fahrenheit = celsiusToFahrenheit(20);
//   console.log (`the fahrenheit is ${fahrenheit}`);

// level 2
// 1.
// function getLargerNumber(num1: number, num2: number): number {
//     if (num1 > num2) {
//       return num1;
//     } else {
//       return num2;
//     }

//   }
//   const largerNumber = getLargerNumber(10, 20);
//     console.log (`the larger number is : ${largerNumber}`);


// level 3
// 1.
// function reverseNumber(num: number): number {
//   let reversed = 0;
//   while (num > 0) {
//     reversed = reversed * 10 + (num % 10);
//     num = Math.floor(num / 10);
//   }
//   return reversed;
// }

// let num = prompt("Enter a number:");
// let reversed = reverseNumber(num );
// console.log(`The reverse of ${num} is ${reversed}`);



