class user {
  constructor(
    public name: string,
    public address: string,
    public email: string,
    public phone: number,
    // public password: string,
    public favoriteColor: string
  ){
  }
  }
const userinfo: user[] = [];
console.log(userinfo);
