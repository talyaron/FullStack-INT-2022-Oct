console.log('hi')

var firstName = 'Michael'
var firstAge = 40

var secondName = 'Dina'
var secondAge = 81

var avg:number = (firstAge + secondAge) / 2

console.log(`${firstName} is ${firstAge} years old and ${secondName} is ${secondAge} years old`)
console.log(`the avrage age between ${firstName} and ${secondName} is ${avg}`)

