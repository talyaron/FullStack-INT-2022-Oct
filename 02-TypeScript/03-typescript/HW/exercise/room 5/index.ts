

var firstName:string = `tomer`;
var firstAge:number= 21;

console.log(`${firstName},${firstAge}`)

var secondName:string = `shlomi`;
var secondAge:number = 35 ;

console.log(`${secondName},${secondAge}`)

var average:number;
