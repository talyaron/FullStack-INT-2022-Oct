const box: HTMLDivElement | null = document.querySelector(".box");
if (box){
        box.style.animationName = "myNewMove"
        box.style.animationDuration = "4s"
        box.style.animationTimingFunction = "2"
    }
